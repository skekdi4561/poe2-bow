# 수정 기록

최신순. 자동 점검 루프(20분 간격)가 남깁니다.

---

## 2026-08-23 아침 — 환율 복구 확인 + annul/chaos 교착 해소

중앙값 수정 후 첫 수집에서 **divine 420 "직접" 정상 복귀** (미끼 10 이 중앙값을 못 움직임 — 실전 증명).
다만 annul·chaos 는 승격 시절의 5000 이 기억에 남아 "급변 의심" 교착(예측했던 패턴) —
1000엑잘 이상은 물리적으로 불가하므로 소급 정정(annul 160, chaos 65). 다음 수집부터 자연 치유.
**남은 설계 과제(미착수)**: 일관된 관측이 N회 연속 거부되면 관측 쪽을 받아들이는 교착 탈출구.
지금은 관측 자체가 중앙값이라 기억 오염 경로가 사실상 막혀 있어 급하지 않음.

---

## 2026-08-23 — 환율 독버섯 3차전: 상주 사기가 기억을 점령한 실사고와 근본 수정

**사고 전말(#12→#17)** 매시간 상주하는 "디바인 10엑잘" 미끼 매물을 min 이 계속 집었고,
급변 방어가 거부하면서도 관측값(obs)을 기억에 넣는 바람에 **세 번 만에 기억의 중앙값이
10 으로 점령**됨 → 03:21부터 10 이 "직접"으로 승격, 세 시간 동안 공개 사이트의 디바인
축척이 40배 왜곡. 같은 시각 로그의 표시값(중앙값)은 380·410 으로 진짜 시세를 보이고 있었다.

**근본 수정 3종**
① 직접 환율 = 매물 중앙값 (min 폐지 — 미끼 소수는 중앙값을 못 움직임. 우회 쌍만 min 유지:
   divine→chaos 는 사기가 과반인 실측이 있어 중앙값이 오히려 위험)
② 거부된 관측은 기억에 안 넣음 — 상주 사기의 점령 경로 차단. 진짜 급변은 중앙값 관측
   자체가 옮겨가므로 따라감
③ 오염 스냅샷 #15~17 소급 정정 (rate 10→400, 정정 사유 how 에 명기 — 관측 기록은 보존)

검증: 미끼 재현([10,380,440]→380), 거부 시 obs 미저장, 기존 스위트 전부 갱신 통과.
복구 수집 즉시 실행 — 다음 푸시부터 공개 사이트 정상 축척.

**교훈** "기억이 관측으로 진화해야 교착이 없다"는 설계는 **상주형 독버섯과 진짜 시세 변동을
구분하지 못한다.** 진화의 입력은 '받아들여진 값'이어야 하고, 관측의 강건성은 그 앞단(중앙값)에서
확보해야 한다.

---

## 2026-08-22 (기능) — v0.2: 게임 위 가격 체크 (전역 단축키 + 곡선 위치 판정)

Ctrl+D → 게임에 Ctrl+C 대신 입력(키 1회=행동 1회, GGG 규칙 내) → 클립보드 파싱 →
시장 곡선 위치 판정 → 커서 옆 topmost 팝업(tk). 전부 표준 라이브러리(ctypes+tkinter).
- 2026-08-24 22:29 [① 사이트 넓은 DPS 렌더] 발견 없음. 라이브 사이트 실측: 차트 X축 195~1,451(=max DPS×1.04), 최대 플롯 DPS 1395, 975개 전부 산점도 표시, Y축 로그(10~1M) 정상 — 클리핑 없음. 위젯(26회)에 이어 사이트도 수집 재설계(→1395) 온전 반영. [오탐 교훈] 첫 프로브가 콤마 라벨('1,451')을 정규식으로 놓쳐 823을 최대로 오인 → 산점도 title 재프로브로 측정도구 결함임을 확인(실제 위반 아님).
- 2026-08-24 22:10 [수집기 잠금 재독해] 도달 가능 결함 발견·수정: read_lock_pid 가 (FileNotFoundError,ValueError)만 잡아, 이 폴더가 OneDrive 라 락 파일이 순간 잠기면 PermissionError 로 수집기가 시작 시 크래시(디렉터리 경로로 재현). 잠금은 안전장치일 뿐이므로 read 는 OSError 전체를 None 처리, acquire 의 write 실패는 크래시 대신 '잠금 없이 진행+경고'로 우아하게 저하. 회귀 테스트(못 읽는 경로→None) 추가. self-test PASS, 재시작.
- 2026-08-24 22:01 [수집기 싱글톤 잠금] 리스크 없는 개선: 두 수집기가 동시에 돌면 거래소 요청 2배→레이트 리밋 위험(오늘 여러 재시작에서 수동 회피). collector.lock(PID) 로 중복 실행을 구조적으로 차단, 강제종료 stale 락은 자동 회수. end-to-end 실측: 2번째 인스턴스 즉시 거부·수집 안 함, 데몬 유지.
- 2026-08-24 22:01 [잠금 pid_alive 버그] 위 잠금을 os.kill(pid,0) 로 짰다가 Windows 에서 살아있는 타 프로세스에 WinError 87 이 떠 '살아있는 데몬을 죽었다고 오판'→잠금 무용지물(실측). ctypes OpenProcess+GetExitCodeProcess 로 수정. own/dead 만 보는 약한 테스트가 이걸 통과시켰으므로, self-test 를 실제 살아있는 자식 프로세스 검증으로 강화.
- 2026-08-24 21:47 [① 위젯 넓은 DPS 범위 렌더] 발견 없음. 수집 재설계로 DPS 범위 781→1395 배증한 데이터를 브라우저 실측 — 매물 960개, 차트/최전선(상단 1395@4500div)/원소지표(361@31div)/필터(960→547) 전부 정상, Vue 오류 0. [관찰·결함 아님] 사이트 조건곡선은 COND_DPS[500-650] 스윕이라 650 상한이나, 위젯은 mods 기반 전범위 필터라 무영향, 조건스윕 확장은 레이트리밋 비용>가치.
- 2026-08-24 19:42 [① serve.py 수집 재설계 재독해] 발견 없음. 3연속 편집(문턱확장→cap30→재설계) 정합 확인: THRESHOLDS[-1] 특례 잔재 없음(재설계가 제거), main 밴드 15→9 로 감소해 레이트 리밋 압박 완화(조건스윕 40+검증자 8 불변). 저-DPS 밴드 트림에도 커버리지 구멍 0 실측(400-600 최대 인접간격 11 DPS·470개, 500-600 최대 밀도 380) — 400/500 앵커+24h합집합+조건스윕이 메움. self-test PASS, 사이클 완주 968개.
- 2026-08-24 19:39 [DPS 지표 정밀 점검] 재설계 사이클 완주: latest.json 775→968개, DPS 범위 204~781 → 204~1395(없던 상반부 복원). 3자 일치 확인(수집기 serve.py·사이트 dpsOf·위젯 appraiser 모두 extended.pdps+edps 동일 소스). 카오스 제외는 의도(활 미사용, 465중 10개만 보유). 밴드필터 간극(344 vs 400밴드)의 원인=품질: 트레이드 dps 필터는 품질20 정규화, 우리 저장값은 실제품질 → 저품질 활서 우리 값 낮음(카오스 아님). 곡선 오염 없음(각 활 자기 DPS에 정확 배치). [결정거리] Q20 표시 vs 실제품질 표시 — 현재 실제품질(트레이드 매물과 일치), 대부분 엔드게임 활은 Q20이라 차이 작음.
- 2026-08-24 19:08 [수집 전략 후속] 고-DPS 밴드(>=660) fetch 상한 100→30. 트레이드 API dps 필터가 우리 pdps+edps 와 다른 지표(품질20/카오스 추정)라 'dps>=780'에 341개 매칭→96개 fetch로 레이트 리밋 반복 도달(실측). 최저가 30개면 곡선 꼭대기 충분. self-test PASS, 수집기 재시작.
- 2026-08-24 18:43 [수집 전략] 사용자 분업 결정 반영: 저-DPS/저가는 크라우드(사용자 가격검색)가, 고-DPS 끝단은 수집기가 전담. THRESHOLDS 상단을 660→780 확장(690/720/750/780 추가) — 실측 DPS>=660 에 125개가 몰려 660 단일 밴드(가격 오름차순 100)로는 비싼 고-DPS 매물 25개가 잘렸음. 각 고-DPS 층은 100개 미만이라 문턱 추가로 통째 수집. self-test PASS, 수집기 재시작.
- 2026-08-24 18:31 [① recent_rows 24h 합집합 dedup] 발견 없음. (cond,lid) seen-set + 지문 dedup 건전, 회귀 테스트 완비(재등록/겸용활/재관측). [관찰·재조사 불필요] 지문이 가격 제외→롤 동일 다른매물이 가격만 다르면 하나 버려짐, 단 주석대로 Rare 전용 가정(레어 롤 충돌≈0)이고 위젯은 비-Rare 무조건 제외(appraiser:256)·사이트는 rareOnly 체크박스라 곡선 영향 없음. 가격 제외는 재등록 최신가 유지의 의도된 설계.
- 2026-08-24 18:27 [②③ path.join 전수 스윕] 발견 없음(21·22 수정이 공격표면 전체 커버 확인). HTTP 도달 가능한 path.join(dir,외부입력) 3곳: server.ts:54 GET(22 수정)·file-uploads GET(21 수정)·POST 쓰기(md5+extname 구조상 안전 — extname 은 경로구분자 미포함, 적대 URL 6종 실측 탈출 0). 나머지 path.join(ConfigStore/GameConfig/GameLogWatcher/AppTray/아이콘)은 app.getPath/__dirname/로컬설정 기반 내부경로라 원격 미도달.
- 2026-08-24 18:08 [② server.ts 정적 서빙] 21회차와 같은 계열 경로조작 발견·수정. 메인 정적 파일 라우트(45행 fs.createReadStream(path.join(__dirname, req.url)))가 /../../.. 로 __dirname(앱 리소스) 탈출(node 실측) — /uploads 만 고치고 이 기본 라우트를 놓쳤음(복제 결함). path.resolve 격리 체크(밖이면 403) 추가, 정상 에셋 경로 해석은 기존과 동일(실측 허용/traversal 차단). 원격 유출은 여전히 localhost+랜덤포트+무CORS 로 막혀 있으나 배포 심층방어. 메인 빌드 OK, 실행 교체 대기.
- 2026-08-24 17:50 [③ 파일 업로드 경로] 확인된 경로 조작(EE2 원본 file-uploads.ts) — GET /uploads/ 가 URL 접미사를 path.join 에 정규화 없이 넘겨 ../../.. 로 uploadsPath 탈출(node 실측). 현재 유출은 localhost+랜덤포트+무CORS 로 막혀 있으나(응답 본문 크로스오리진 판독 불가), 배포 바이너리 심층방어로 path.resolve 격리 체크(target 이 uploadsPath 밖이면 403) 추가 — 정상 md5.확장자 파일명은 영향 0(실측 허용/traversal 차단). 메인 빌드 OK, 실행 교체 대기.
- 2026-08-24 17:28 [① harvest.ts 재독해] 발견 없음. 13회차 경쟁조건 수정 완결성 재검토: 각 행이 큐 적재 전 normalizeResult 로 league 를 구워 저장→전역 _queue 에 여러 검색 행이 섞여도 무해(근본 수정 확인). _flush 재예약(1s) 중 harvestFetchResults 재호출 시 !flushTimer 가드로 이중예약·행유실·타이머누수 없음. ctx.league(요청시점)+language(수집시점) 이중게이트가 pc-ggg Standard 같은 비-카카오 Standard 를 올바르게 제외.
- 2026-08-24 17:08 [① worker 청소·배치 경계] 발견 없음. 정적: SELECT-then-INSERT 는 INSERT OR IGNORE 가 진짜 중복제거라 동시요청 안전, slice(60) 이 map 앞이라 대량배열 바운드, 바인드 파라미터로 주입 불가. 동적(라이브,SMOKE): 배치내 동일id/빈배치/rows 비배열/깨진JSON(400)/200개→60상한 5종 크래시·과대작업 없음. within-batch written 카운트 1 차이는 디버그 필드라 미수정.
- 2026-08-24 16:48 [② focus-overlay 상태머신] 발견 없음. assertOverlayActive/assertGameActive 는 현재 상태로 가드된 멱등 전이 — F7 여닫기+Ctrl+D 섞어 반복해도 상태 안 꼬임(node 시뮬 확인). [관찰·차기 게임실행 시 확인] 위젯 F7 닫기가 focus-game 으로 오버레이 전체를 게임으로 되돌려, 가격창 동시 개방 시 그것도 클릭통과됨 — 단 원본 패턴(가격창도 닫을 때 focus-game, 단일창 포커스 설계)과 일치·우리 회귀 아님, 2위젯 상호작용은 Electron 메인 필요라 이 세션서 재현 불가.
- 2026-08-24 16:29 [① index.html DOM 생명주기] 발견 없음. partition/staleKept 폴백을 node 로 재현·실측 — 전부 신선/전부 만료(빈화면 대신 매물+경고)/혼합(신선 우선)/데이터 없음(만료와 구분)/t 누락(만료 취급 폴백)/d·p=0(무효 제외) 6종 전부 우아하게 처리. 수집 멈춤 시 빈 화면 고장으로 안 보이는 저하가 정확. (참고: 로컬 8099 는 다른 프로젝트 서빙 중이라 파티션 로직만 격리 검증).
- 2026-08-24 16:09 [① 환율 독버섯 방어] 발견 없음(방어 견고). guard_rates/solve_rates/best_offer 는 두 실사고(#15-17 하락미끼·00:37 상승미끼) 회귀 테스트 완비 확인. 커버리지 공백 하나 보강: rate_memory 의 r>=1 필터가 guard_rates 의 m 을 항상 >=1 로 보장(=m*3 0-오염 차단)하는 불변식이 미검증이었음 — 이 필터 제거 시 깨지는 임시DB 테스트 + 빈 기억 통과 + 정확히 3배 경계 통과 3종 추가. self-test PASS.
- 2026-08-24 15:48 [③ 게임 로그 감시] 발견 없음(EE2 원본, 우리 포크서 기본 비활성 readClientLog:false). readToEOF 는 64KB 청크·offset 단조증가·EOF 종료라 무한루프/메모리 누적 없음. 64KB 경계 UTF-8 멀티바이트 분할은 원본 동작·미관 수준(크래시 아님)이라 미변경 상류 코드로 수정 대상 아님.
- 2026-08-24 15:29 [① index.html 예산·환율 경계] 발견 없음. posToBudget(pos 클램프·lo==hi NaN 없음), budgetRange null+2행미만 980행 가드로 posToBudget 크래시 차단, lo 는 p>0 필터로 항상 양수(0나눗셈 불가), dpsAt 은 예산부족·빈배열·NaN 에 null 안전, money() 디바인 자동전환 정확(300ex=1div 경계·음수 절대값·환율0 시 ex 유지) — 전부 node 실측 확인.
- 2026-08-24 15:11 [① harvest 훅 통합] 경쟁 조건 수정: 수집 문맥을 모듈 전역 ctx 로 두고 async 응답 시점에 읽어, 검색A(검) 직후 검색B(활)를 하면 A 응답 도착 시 ctx 가 활로 바뀌어 검(pdps 존재)이 활 데이터로 오염 업로드됐다(node 재현 확인). 전역 ctx 제거→HarvestCtx 를 requestResults opts 로 요청마다 고정 전달(값이라 서로 안 섞임). vitest 2종 추가, 빌드+미리보기 회귀 없음(772개), 실행 교체 대기.
- 2026-08-24 14:48 [③ 클립보드 파서] 발견 없음(EE2 원본, 미변경). 정적: parseClipboard 전체 try/catch(예외→graceful err), 유일 while(!stat.done)의 전 분기가 statIterator.next() 로 전진→무한루프 불가. 동적: 미리보기에서 기형 입력 7종(빈/구분선만/5만줄반복/유니코드폭탄/널바이트/숫자없는mod) 실측 — 전부 <10ms 안전 처리, hang/크래시 없음, 5만줄 9.8ms 로 O(n²) 없음 확인.
- 2026-08-24 14:28 [② 단축키 등록] 발견 없음. F7 market-curve 토글이 Shortcuts.ts 등록과 정합: F7 은 예약 게임키 집합에 없음(자유), UiohookKey.F7=65 유효(keepModKeys 릴리스 정상), 액션은 toggleKey truthy 시만 push(빈키 등록 없음), dedup 안전. [UX 한계·결함 아님] market-curve 는 키 재설정 UI 가 없어 충돌 시 config 수동편집 필요 — 현재 F7 비어 있어 무해, 개선 여지로만 기록.
- 2026-08-24 14:08 [① /recent↔merge 소비 경계] 발견 없음. 워커 보존48h/제공24h·최신3000 vs serve 소비24h·최대5000 창 정합 확인. 경계 t 7종 주입(안쪽/직후/t==cut 경계/직전/48h/t누락/t문자열) — 잘못·누락 t 는 항상 제외(fail-safe), 경계는 t<cut 이라 포함. [의도된 특성·재조사 불필요] 크라우드 t 는 INSERT OR IGNORE 로 최초관측에 고정→계속 팔려도 24h 후 크라우드 표본서 빠짐(24h 신선도 목표 부합, 수집기 스윕이 최전선 보완).
- 2026-08-24 13:48 [① 위젯 차트 수치 엣지] 발견 없음. niceTicks 를 극단 입력(동일 DPS·초근접·1~10만 범위)으로 스트레스 — 틱 최대 8개, 무한루프/NaN 없음(step 0 은 mag 언더플로 1e-324 필요라 도달 불가). log 스케일 동일가격(lo==hi)은 ||1 로 0 반환. 라이브 미리보기에서 필터로 매물 0개까지 좁혀도 '부족' 문구 정상·canvas 생존·위젯 오류 없음(501 은 3회차 SSRF 차단 정상 작동).
- 2026-08-24 13:29 [③ 스로틀/레이트 리밋] 실결함 없음(안전 확인). 진위검증자(verifier)의 레이트 리밋 안전 속성을 함수 주입으로 실측 — 예산 8회 네트워크 상한 준수, 캐시로 재검증 시 네트워크 0, 실재 매물 통과, 가격 조작 거부, TradeError 는 fail-closed. 사용자 최우선 제약(자는 동안 레이트 리밋 금지)을 지키는 신규 코드인데 회귀 테스트가 없던 커버리지 공백 → serve.py self-test 에 예산/캐시/가격일치 검증 5종 못박음.
- 2026-08-24 13:10 [① appraiser 스냅샷 파싱] latest.json 읽기 경계에서 pdps/edps/price 를 타입 검사 없이 `?? 0` 만 써서, 숫자 아닌 값이 오면 pdps+edps 가 문자열 결합("227"+38="22738")돼 frontier 정렬을 오염(실측 재현). 4회차 '경계마다 숫자 강제' 원칙이 빠졌던 읽기 경계 — numOr0 로 강제, rowsFromSnapshot 순수 함수로 추출해 극단값 주입 vitest 2종 추가. 현 파이프라인(serve/worker 숫자 보장)에선 미도달이나 심층 방어. 빌드+미리보기 회귀 없음(매물 776개 정상), 실행 교체 대기.
- 2026-08-24 12:48 [② 설정 마이그레이션+IPC] 발견 없음. configVersion 35 위젯 추가 마이그레이션의 wmId 계산이 EE2 원본 6개 마이그레이션과 동일 패턴(손상 설정 NaN 은 원본 동작·재현 불가라 수정 안 함), 신규(defaultConfig)/기존(마이그레이션) 두 경로 위젯 형태 일치 확인. focus-overlay 이벤트 이름 3파일(ipc/types·OverlayWindow·위젯) 일치 + app.asar 포함 확인.
- 2026-08-24 12:29 [③ 렌더링 XSS] 발견 없음. 크라우드 옵션/이름 문자열이 위젯·사이트에 표시되는 전 경로 점검 — 위젯은 v-html 전무(Vue {} 자동 이스케이프), index.html 은 모든 innerHTML 삽입부에 esc() 적용(shortMod/name/옵션키/data-key). 공격 페이로드 4종(img onerror, script, 마크업 삽입, span 탈출)을 modKey+esc 실제 파이프라인에 통과시켜 전부 &lt; 로 무해화 확인.
- 2026-08-24 11:50 [① worker 입력검증] 숫자 필드에 상한이 없어 pdps:1e300 같은 값이 통과 → 곡선 DPS 축 파괴(merge_harvest 최종 관문까지 침투 실측). 워커 validRow + serve.py merge 양쪽에 상한(dps 10만/aps 100/crit 100/price 1e9) 추가, fee=0 은 null 처리(fee 게이트 우회 차단). 워커 배포+스모크(거대행 accepted 0, 혼합 1), serve self-test 통과. CORS(*)·body 300KB·slice(60)-before-map·바인드 파라미터는 안전 확인.
- 2026-08-24 11:30 [② 로컬 프록시 호스트 검증] 업스트림 main/src/proxy.ts 는 견고함 확인(split 추출 host 와 URL 파서 해석이 어긋나는 우회 케이스 7종 실측 — 전부 안전). 실결함은 우리 vite 미리보기 스텁: 호스트 게이트가 `[\w.-]+` 뿐이라 evil.com·169.254.169.254(메타데이터 IP)까지 프록시되던 SSRF(dev 전용) — 공식 거래소 호스트 허용목록으로 잠금, 실서버에서 evil→501/정상→200 실측.
- 2026-08-24 11:11 [harvest.ts 업로드 경계] 한 검색이 100행을 만들 때 60행 초과분이 queue.clear() 로 소리 없이 유실되던 결함 수정 — 배치(30행)로 나눠 이어 전송, vitest 2종 추가 (빌드 완료, 실행 교체 대기). keepalive 64KB 한도는 실측(행 최대 788B, 60행 47KB)상 미초과라 재현 불가로 기록만 — 배치 30행이 여유폭 2배 확보.
- 2026-08-24 10:57 [위젯/지표 변환] 원소·물리 지표에서 해당 DPS 가 0인 활이 곡선에 샐 수 있던 잠복 결함 수정 — metricRows() 로 추출해 index.html 의 v.d>0 규칙과 일치시킴, vitest 추가 (오버레이 재빌드 완료, 실행 교체 대기). 같은 회차: 수집기 첫 실전 사이클에서 환율 독버섯 가드 정상 작동 확인(annul 3225·chaos 1 거부).
- 파서·frontier·판정 전부 파이썬 포트, 페이지와 같은 픽스처로 자체검증(파서 6종+판정 5종)
- 판정문: 이 DPS 최저가 / 호가 대비 % / 한 계단 위 / DPS 밖 옵션 / 기준 매물·시각
- `--check-clip` 진단 플래그: 게임 없이 클립보드만으로 전체 파이프라인 검증 가능
- 실전 검증: 실제 시세 163개 기준 판정문 정상 출력, exe 스모크(단축키 등록 로그+ping 200)

**같이 잡은 사고 2건**
① ctypes 64비트 핸들 잘림 — GetClipboardData restype 미지정으로 접근 위반 즉사(재현→수정)
② 21:59 수집분에 엑잘 환율만 실림(교환 API 실패) — 판정에서 디바인 매물 135개가 통째로
   사라졌었음. 이중 수리: 판정기에 기본값 폴백(실사용 화폐만 "환율 일부 기본값" 표시) +
   수집기가 빠진 환율을 지난 스냅샷에서 이월(merge_rates, "이전 수집분" 표기). 검증 6종.

---

## 2026-08-22 (기능) — 사이드 프로그램 v0.1: 단일 exe (POE2 전용)

방향 확정: POE Overlay 를 넘는 사이드 프로그램 (수집은 사용자, 분류는 자동 파이프라인).
v0.1 = serve.py 를 PyInstaller 단일 exe(9.6MB)로 포장.
- `_resolve_root()`: exe 모드에서 자산은 번들에서 exe 옆 "감정소데이터" 폴더로 복사,
  DB·시세도 거기에 (임시폴더에 쓰면 재시작마다 증발하는 함정 회피)
- `bootstrap_latest()`: 시세가 없으면 공개 사이트에서 한 번 받아옴 — 갓 설치해도 곡선이 바로 보임
  (거래소가 아니라 우리 github.io 라 레이트 리밋 무관). 이미 있으면 네트워크 안 탐(검증 추가)
- `--nobrowser` 플래그 (자동화·테스트용)
- 빌드: `python -m PyInstaller --onefile --name 활시세감정소 --add-data ...` / 산출물은 .gitignore
실행 검증: 데이터 폴더 생성·자산 6종 복사·공유 시세 수신·index/ping 200 전부 확인.
다음: v0.2 = 전역 단축키 가격 체크 + "시장 곡선 위 위치" 표시 (차별화 핵심). POE1 은 추후.

---

## 2026-08-22 (기능) — 표본 5,000개 준비: frontier O(n log n) + 밴드당 25개

- frontier 를 전수 비교 O(n²) → DPS 내림차순 정렬 후 한 번 훑기로 교체.
  실측 5,000개 721ms → 1.4ms. 동점(같은 DPS·가격) 전원 생존 판정까지 기존과 동일(검증 4종).
- 수집 밴드당 기본 개수 10 → 25 (검색 요청 불변, fetch 예산 실측 내). 표본 목표 5,000의 1단계.
- 합집합 상한 2,000은 유지 — 차트 SVG 점 수가 다음 병목이라 실측 후 상향 예정.

---

## 2026-08-22 (기능) — 최근 24시간 스냅샷 합집합 (표본 두껍게)

스냅샷 한 장(~160개)만 보여주던 latest.json 을 **최근 24시간 수집분의 합집합**으로 교체.
`normalize()` 가 거래소 매물 id 를 보존하고(DB `bows.id` 컬럼 이관), `recent_rows()` 가
같은 id 는 최신 관측만 남기며 24시간 밖은 자른다(상한 2,000 — frontier O(n²) 보호).
페이지는 매물별 `t` + ROW_TTL 로 이미 준비돼 있어 무변경 수용. 예산표의 옵션별 최전선은
`dataRev` 캐시로 슬라이더 드래그 시 재계산 안 함(표본 늘 때 성능 절벽 방지).
기준 시각 줄에 "최근 24시간 관측 합집합" 명시(시각 2시간 이상 벌어질 때).
실측 근거: 오늘 6회 수집 합집합 347개, 시간당 수집 시 24시간 500~900개 추정.
주의: 기존 스냅샷엔 id 가 없어 다음 수집부터 몇 시간에 걸쳐 점점 두꺼워진다.
검증: recent_rows 4종(창 밖 잘림/같은 id 최신만/무id 최신 스냅샷만/매물별 시각) + 3종 suite PASS.

---

## 2026-08-22 (기능) — 모든 매물 24시간 만료 (ROW_TTL)

스냅샷·채집·직접 등록 전부 수집 시각 기준 24시간이 지나면 통계·표에서 빠진다
(`partition()` 중앙 필터 + "수집한 지 24시간이 지난 N개 제외" 계수). 예외 하나:
**스냅샷 전체가 만료된 경우**(수집기 정지)는 빈 화면 대신 매물을 유지하고 freshness 가
"N일 전 시세입니다 — 통계 신뢰도가 낮습니다" 경고를 크게 띄운다(`staleKept`).
채집 TTL 은 기존 48시간에서 공통 24시간으로 통일. 브라우저 실측 4시나리오
(정상/낡은 수동 매물 제외/수집 멈춤 경고/채집 만료 걸러짐) 전부 통과, self-test 에 isFresh 4종.

---

## 2026-08-22 (기능) — 공개판 다듬기 4종

- **1디바인부터 자동 div 표기** — `money()` 가 자동 모드에서 값마다 ex/div 를 가른다(1딥 경계).
  "590,800 ex" 류 여섯 자리 사라짐. 수동 화폐 선택은 그대로. 가격 헤더는 자동일 때 단위 생략.
- **조건 다중 선택** — 라디오→체크박스(`pickedConds` Set). 단일 측정 조건은 검색 곡선 그대로,
  겹치면 옵션 실측값 로컬 판정(`condPredicates`/`meetsAll`)으로 전환하고 "표본이 얇습니다" 경고.
  단일 로컬 조건의 문턱 막대·포커스 복원(18회차) 유지.
- **예산표 롤 구간 세분화 + 스크롤 + 정렬** — 표본 조건을 관측 분포 사분위(25/50/75%) 문턱으로
  최대 3행씩 나눔. `.oplist` max-height 520px 스크롤, DPS 높은/낮은 순 토글. 빈칸(#) 라벨은
  `condText()` 로 문턱값 채움 + serve.py 빈도 라벨도 수집 시점에 채우게 수정.
- **공개판 정리** — 예시 데이터/전부 지우기 버튼 제거, 실행기 없으면(ping 실패) 거래소 URL
  입력줄 통째로 숨김(실행기로 열면 그대로 보임).

검증 3종 PASS + 브라우저 실측(공개 모드): 다중 선택 겹침 5점/경고 표시/전부 해제/막대 유지,
표 ex 12·div 150 혼합 표기, 예산 최대 1,477 div, 정렬 토글 [781,781,668]↔[551,651,652], 콘솔 오류 0.

---

## 2026-08-22 (배포 전 점검) — 공개 배포 위협 모델로 전체 재점검

**고침 · 채집 행의 mods 가 문자열이면 글자 단위로 쪼개져 조건 목록 오염** — 채집 저장값은
외부 입력(다른 스크립트도 localStorage 를 쓴다)인데 `offMods` 의 for...of 가 문자열을 글자별로
돌린다. `harvestRows` 가 배열 아닌 mods 를 비우고, id 는 문자열만 받게 강화. 브라우저 실측:
한 글자짜리 조건 오염 0건.

**고침 · 채집 매물 무기한 잔류** — "최근 것만 남기기"를 눌러도 새로고침이면 다리에서 되살아났다.
48시간 만료(`HARVEST_TTL`)로 해결. 실측: 사흘 전 매물이 부팅에서 걸러짐.

**고침 · 채집기의 크기 상한 부재** — 페이지 세계는 가짜 이벤트를 쏠 수 있다. 이름 120자,
옵션 40개×200자 상한. 거대 문자열로 GM/localStorage 를 부풀리는 경로 차단.

**고침 · 설치 링크 절대경로** — GitHub Pages 하위경로(`/repo/`)에서 깨짐. 상대경로로.

**배포 차단 발견 · snapshots.db 에 내 저장 검색 URL** — 저장소에 올리면 공개된다.
`.gitignore` 생성(snapshots.db, __pycache__, *.tmp). **커밋 전 반드시 확인할 것.**

**깨끗함 확인** — latest.json: 계정·귓속말·개인 식별자 없음(매물 9필드뿐).
공개 후보 파일 4종에 사용자명·로컬 경로 0건. XSS: 채집 이름/옵션에 img onerror 삽입 →
실행 0, 요소 생성 0, 이스케이프 표시. 가격이 문자열인 행은 "제외 1개"로 계수(조용히 안 사라짐).
세 검증 전부 통과(harvester 19종 / index / serve).

---

## 2026-08-22 (기능) — 채집기(유저스크립트) 추가 · 루프 밖 작업

**새 파일 `poe2-bow-harvester.user.js`** — Tampermonkey 용. 거래소 페이지가 자기 API 에서
"이미 받아온" 응답(fetch/XHR)을 옆으로 복사해 GM 저장소에 쌓고, 감정소 페이지(localhost:8731)에서
localStorage `poe2harvest` 로 건네준다. **거래소로 요청을 새로 만들지 않는다 — 추가 부담 0.**

- 활 판정: 속성 클래스 정확 일치(활/Bow — 쇠뇌의 "Crossbow" 부분 일치 함정 회피) 또는
  weapon.bow 카테고리 검색 결과 id. 둘 다 아니면 안 줍는다. 등급 불명·규격 밖 화폐도 버림
  (serve.py normalize 와 같은 기준).
- 페이지: `harvestRows`/`loadHarvest` — snap 과 같은 갈아끼우기 패턴(hv 표식), 깨진 저장값 무시.
  부팅 시 스냅샷 뒤에 합류, `poe2harvest-updated` 이벤트로 실시간 갱신.
- serve.py: SERVED 에 `/poe2-bow-harvester.user.js` 추가(설치 링크용).
- 검증: `node poe2-bow-harvester.user.js --test`(normalizeRow 15종 + HOOK), index self-test 에
  harvestRows 잡값 5종, 브라우저에서 HOOK 실제 주입→가짜 응답→정규화까지 전 구간 확인
  (활 검색만 통과, 쇠뇌 카테고리 차단, 부팅 병합 116+3 중복 0, 깨진 값 3종 무시).
- **주의**: index self-test 의 node 하니스에 이제 `window` 스텁이 필요하다
  (`global.window={addEventListener(){}}`) — 최상위 이벤트 리스너가 생겼기 때문.
- 미구현(알림): 거래소 CSP 가 훅 주입을 막는 경우의 대체 경로 — 실제로 막히는 게 관측되면 추가.
  콘솔 경고로 감지 가능. 사용자 간 공유(풀링)는 다음 단계.

---

## 2026-08-22 14:4x — 21회차 · 영역: 치명타 계산 / 시각 표시 / 첫 실행 · **이번 회차 발견 없음**

코드 변경 0, 아티팩트 재배포 없음. 본 것과 확인 방법:

**치명타 반영 계산 (`partition()` 의 배율)**
`크리 확률 × 치명타 피해 보너스` 로 DPS 를 키우는 자리에 값을 7가지 넣어봤다.

    100 -> 최고 DPS 781.1 에서 854.3      0 / 빈칸 / "abc" -> 781.1 (배율 1, 안전하게 무시)
    -500 -> 530.9 (0 밑으로는 안 감)       1e9 / 999999 -> 계산은 되지만 표시만 커짐

예외 0건, 매물이 조용히 사라지는 일 0건(제외 카운터 계속 0). 관측 crit 이 5~9.99% 라
음수 보너스로도 배율이 0 밑으로 못 내려간다 — 지어낸 값이 곡선에 섞일 경로가 없다.

**시각 표시 함수 (`agoStr`/`timeStr`/`dayStr`)** 15가지 입력으로 훑었다.
시계가 어긋나 **미래 시각**이 와도 "-5분 전" 같은 게 안 나오고 "방금 전"으로 떨어진다.
59분/60분/23시간/24시간 경계 전부 정상. `undefined`/`NaN`/문자열은 `NaN월 NaN일` 을 만들지만,
**표시 경로에 닿지 않는다** — `freshness()` 가 `.filter(Boolean)` 로 먼저 걸러내고 "시점 불명 N개"
로 따로 센다. 남은 한 곳(`loadSnapshot` 의 상태줄)은 우리 수집기가 쓴 값만 받는다. 결함 아님.

**첫 실행(수집 전, latest.json 없음)**
`latest.json` 404 인 폴더로 띄웠다. 매물 0, "아직 등록된 활이 없습니다.",
차트·계단·예산카드가 각자 안내문/숨김으로 정확히 빠지고 콘솔 오류 0.
그 상태에서 "예시 데이터 넣기"를 눌러도 정상(5개, 차트·계단 생성).

**헛짚은 것 두 번(기록)**
①`#fail` 의 textContent 가 "계산 자체 검증 실패" 라 자체 검증이 깨진 줄 알았다 — `display` 를
확인하니 `none`, 크기 0. **감춰진 요소의 textContent 를 상태로 읽으면 안 된다.**
②시각 함수가 `NaN월 NaN일` 을 내는 걸 결함으로 볼 뻔했다 — 호출부를 따라가 보니 그 값이
표시까지 가는 경로가 없었다. **함수 단독 출력이 아니라 호출부까지 봐야 판정이 된다.**

**남긴 관찰(고치지 않음)** 17회차에 화면 경로에 스로틀을 붙이면서, fetch 배치를 도는 중간에
한도에 걸리면 `TradeError` 가 올라가 **이미 받아온 매물까지 통째로 버려진다.** 요청은 이미
썼으니 손해다. 다만 부분 결과를 조용히 돌려주면 "덜 왔다"는 걸 사용자가 모르는 문제가 새로
생기므로, 어느 쪽이 옳은지 단정할 근거가 없어 손대지 않았다. (연타 5회쯤이면 닿는 경로)

---

## 2026-08-22 14:2x — 20회차 · 영역: DB 스키마 / 장기 운영 · **7회차 수정의 회귀를 스스로 잡음**

**고침 · 서버가 latest.json 을 내보내는 중이면 수집 결과가 버려진다 (윈도우)**
7회차에 "쓰다 실패해도 지난 스냅샷이 안 깨지게" 하려고 `.tmp` 에 쓰고 `os.replace` 하도록
바꿨는데, **윈도우는 대상 파일을 누가 읽는 중이면 `os.replace` 를 WinError 5 로 거절한다.**
문서에 적힌 사용법이 서버와 수집기를 같이 띄우는 것(`serve.py` + `serve.py --collect --every`)
이라, 겹치는 게 드문 일이 아니다. 실제 HTTP 서버가 `latest.json` 을 계속 내보내는 동안 측정:

    고치기 전   쓰기 성공 20 / 40   (PermissionError [WinError 5] 20번)
    고친 뒤     쓰기 성공 40 / 40   (재시도 대기 포함 총 0.9초)

데이터가 깨지진 않는다(원본은 그대로 살아 있다 — 7회차 수정의 목적은 지켜졌다). 대신 **그 시간의
수집분이 통째로 사라지고** 로그에 `수집 오류: PermissionError` 만 남는다. 한 시간에 한 번 도는
작업이 절반쯤 헛돈다는 뜻.
→ 읽는 시간은 밀리초 단위라 0→0.05→0.1→…→1.6초로 잠깐씩 기다렸다 다시 건다. 끝까지 안 되면
그대로 예외를 올린다(원본은 어차피 안 건드려졌다). 읽기 실패 0, `.tmp` 잔여물 없음도 확인.

**교훈으로 남긴다** — "원자적 교체"는 POSIX 기준 상식이라 윈도우에서 되는지 안 재봤다.
`os.replace` 는 원자성은 주지만 **대상이 열려 있을 때의 동작이 OS 마다 다르다.**
직전 회차에 넣은 코드도 다음 회차에 다시 의심해볼 것.

**검증 추가** — 다른 스레드가 파일을 0.35초 붙잡고 있는 동안 `write_latest` 를 부른다.
재시도가 빠지면 그 자리에서 PermissionError 로 터진다.

**문제 없던 것 — DB 스키마 마이그레이션**
옛 DB 다섯 가지로 `db()` + 실제 INSERT 까지 돌려봤다.

    최초 스키마(rates·cond 둘 다 없음)  OK  기존 행 1개 그대로 유지
    중간 스키마(cond 만 없음)            OK
    파일 없음 / 0바이트 파일             OK  (새로 만듦)
    DB 가 아닌 텍스트 파일               DatabaseError 로 거절 — 덮어쓰지 않는 게 맞다

`ALTER TABLE` 이관이 기존 데이터를 건드리지 않고, 이관 후 새 컬럼(`rates`/`cond`)까지 정상 삽입된다.

**남긴 관찰** DB 는 무한히 쌓인다(한 시간마다 스냅샷 하나, 하루 ~2,800행). 한 달이면 수 MB
수준이라 지금은 문제가 아니고, 오래된 스냅샷을 지우는 기능은 없다. 필요해지면 그때.

**index.html 은 이번 회차에 변경 없음 — 아티팩트 재배포 안 함.**

---

## 2026-08-22 14:0x — 19회차 · 영역: HTTP 응답 헤더 / 오류 응답 경로

**고침 · 모든 오류 응답이 한 줄도 못 쓰고 연결이 끊긴다 (요청 핸들러가 매번 죽음)**
헤더를 재려고 여러 경로를 훑다가 **응답이 아예 안 오는 것들**을 발견했다:

    GET /favicon.ico     Empty reply from server
    OPTIONS /api/trade   Empty reply from server
    POST /api/trade      Empty reply from server

stderr 를 열어보니 요청마다 트레이스백이 찍히고 있었다:

    File "serve.py", line 725, in log_message
        if "/api/" in (args[0] if args else ""):
    TypeError: argument of type 'HTTPStatus' is not a container or iterable

`log_message` 가 `args[0]` 을 **항상 문자열로** 가정했는데, `send_error` 는
`log_error("code %d, message %s", HTTPStatus.X, msg)` 로 **HTTPStatus 열거형**을 넘긴다.
거기서 난 TypeError 가 `send_error` 밖으로 터져 나가 **응답을 쓰기 전에** 핸들러가 죽고
소켓이 그냥 닫힌다. 즉 404·501·400 **모든 오류 경로가 통째로 망가져 있었다** —
정상 200 만 문자열 인자를 받아서 지금까지 안 드러났던 것.

`/favicon.ico` 는 `SERVED` 화이트리스트에 있는데 파일이 실제로 없어서 404 로 가는 자리라,
**브라우저가 페이지를 열 때마다 자동으로 요청해 매번 핸들러 스레드를 하나씩 죽이고 있었다.**
→ 문자열일 때만 `/api/` 필터를 적용하고, 문자열이 아니면(=오류 로그) 그대로 남긴다.
오류를 조용히 삼키지 않는 쪽이 맞다. 재확인:

    GET /favicon.ico   404      OPTIONS /api/trade  501      POST /api/trade  501
    GET /snapshots.db  404      GET /없는파일        404      GET /index.html  200
    서버 로그의 트레이스백: 0개 (고치기 전에는 요청마다 하나씩)

**검증 추가** — `log_message` 에 `HTTPStatus` 를 넘겨도 안 터지고 오류 문구가 로그에 남는지,
정적 요청(`/index.html`)은 여전히 조용하고 `/api/ping` 은 남는지.

**문제 없던 것 — 응답 헤더**
`Access-Control-Allow-Origin` 이 **어디에도 없다**(Origin 을 붙여 요청해도 안 붙는다) —
2회차의 Host 검사와 합쳐서 다른 사이트의 스크립트가 `/api/trade` 응답을 읽을 수 없다.
`/latest.json` 은 `application/json`, JSON 응답은 `charset=utf-8` 명시, `index.html` 은
`text/html` + 페이지에 `<meta charset="utf-8">` 이 있어 한글이 깨지지 않는다.

**index.html 은 이번 회차에 변경 없음 — 아티팩트 재배포 안 함.**

---

## 2026-08-22 13:4x — 18회차 · 영역: 키보드 조작 / 포커스

**고침 · 조건 목록을 키보드로 한 칸 넘긴 뒤엔 더 못 움직인다 (기능 자체가 막힘)**
라디오 그룹은 화살표 키로 옮길 때마다 `change` 가 뜨는데, 그 핸들러가 `buildConds()` 로
`#filters` 를 innerHTML 통째로 다시 그린다. 그러면 **방금 포커스가 있던 라디오가 문서에서
사라져** 포커스가 `body` 로 떨어진다. 브라우저 실측:

    라디오 첫 항목에 포커스        INPUT[name=cond]
    다음 조건 선택 직후 포커스     BODY          <- 여기서 끝
    그 라디오가 아직 문서에 있나   false

포커스가 body 면 다음 화살표 키는 아무 데도 안 간다(페이지만 스크롤). 즉 **키보드만 쓰는
사용자는 조건 목록에서 첫 한 칸만 움직이고 갇힌다.** 취향 문제가 아니라 기능이 막히는 결함.
→ 다시 그린 뒤 지금 선택된 라디오로 포커스를 되돌린다(`focusChecked`, `preventScroll`).
마우스로 눌러 포커스가 원래 그 라디오에 없던 경우엔 뺏지 않도록 `hadFocus` 로 가렸다.
재확인: **화살표 4연속 이동 4/4 성공**(고치기 전 1회에서 멈춤).

**고침 · 매물 삭제 때마다 포커스가 body 로 떨어진다**
`refresh()` 가 표를 다시 그려서 누른 × 버튼이 사라진다. 여러 개를 지우려면 매번 Tab 으로
표까지 되돌아와야 했다. → 같은 자리(없으면 마지막) 삭제 버튼으로 옮긴다.
재확인: **연속 3회 삭제 3/3 동안 포커스가 삭제 버튼에 유지**.

**고침 · 예산 슬라이더에 이름이 없다** 스크린리더가 "슬라이더" 로만 읽던 자리 —
`aria-label="예산"` + `aria-describedby="budget-val"`(현재 금액)을 붙였다.

**검증 추가** — `focusChecked()` 가 되돌릴 라디오가 없을 때 조용히 `false` 를 돌려주는지.
*포커스 이동 자체는 DOM 이 있어야 재는 것이라 node self-test 로는 표현이 안 된다 —
실제 근거는 위 브라우저 실측이다.* 이 한계를 숨기지 않고 적어둔다.

**문제 없던 것 — 나머지 접근성**
탭으로 닿는 요소 161개. **클릭 커서인데 포커스를 못 받는 요소 0개**(가짜 버튼 없음).
차트에 `role="img"` + `aria-label="DPS 대비 가격 산점도"`, 삭제 버튼에 `aria-label="삭제"`.
`#budget` 외에 이름 없는 입력 없음. 예산 슬라이더·단위 선택 같은 고정 요소는 조작해도
포커스가 그대로 남는다(다시 그리지 않으므로).

---

## 2026-08-22 13:2x — 17회차 · 영역: 동시성 / 화면 경로의 레이트 리밋

**고침 · 화면의 "불러오기" 는 스로틀을 아예 안 거친다 (IP 차단으로 이어지는 경로)**
수집기는 요청마다 `throttle()` 로 간격을 재는데, 사람이 누르는 `load_trade` 경로에는
호출이 **한 군데도 없었다**. 대역한 `api_get` 으로 실제 요청 수를 세어보니:

    불러오기 한 번(개수 100) -> 거래소 요청 12개 (검색 GET+POST 2 + fetch 10)
    그중 throttle() 을 거친 것: 0개
    실측 한도: fetch 4초당 12회 / 12초당 16회, search-post 10초당 5회

`PAUSE=0.7` 초만 두고 fetch 10개를 7초에 몰아 보내므로 **한 번만 눌러도 12초 창(16회)의
대부분을 쓰고, 두 번 누르면 넘긴다.** 429 다음은 30분 차단이다 — 자동 수집은 조심스럽게
설계해놓고 정작 사람이 연타하는 경로가 무방비였다.
→ 검색 2회·fetch 배치마다 수집기와 **같은 버킷으로** `throttle()` 을 붙였다. 재확인: 12요청
전부 스로틀을 거친다(검색 2 / fetch 10).
다만 화면 경로가 최대 300초를 자면 브라우저가 멈춘 것처럼 보이므로 `UI_MAX_WAIT=20` 을 줘서,
벽 앞이면 길게 자는 대신 **0.00초 만에** "거래소 요청 한도에 가까워 지금은 쉬어야 합니다…
잠시 뒤에 다시 시도해주세요" 로 답하고 끝낸다.

**고침 · throttle() 이 스레드 안전하지 않아 동시 요청이 한꺼번에 나간다**
`ThreadingHTTPServer` 라 탭 두 개나 연타로 요청이 겹칠 수 있는데, `LAST_CALL` 을 잠금 없이
읽고 썼다. 스레드 3개로 실측:

    고치기 전   +1.00초, +1.00초, +1.00초   (간격 0.00 — 셋이 동시에 나감)
    고친 뒤     +1.00초, +2.00초, +3.00초   (간격 1.00 — 의도대로)

간격을 재는 코드가 있어도 겹치면 무의미해진다. `_THROTTLE_LOCK` 으로 판정·대기·기록을
한 덩어리로 묶었다. 수집과 서빙은 같은 프로세스에 같이 안 뜨므로(main 이 둘 중 하나만 한다)
전역 락 하나로 충분하다.

**검증 추가** — ①`load_trade` 가 검색 2회·fetch 3배치(25개)에 대해 스로틀을 부르는지
②스레드 3개가 겹쳐도 마지막-처음 간격이 1.5초를 넘는지(락이 풀리면 즉시 실패한다).

**헛짚은 것(기록)** 고친 뒤 재확인에서 동시 요청이 여전히 `[0.0, 0.0, 0.0]` 으로 나와
락이 안 먹는 줄 알았다. 원인은 **같은 프로세스 앞부분에서 내가 `serve.time.sleep` 을
대역해둔 것**이었다(`serve.time` 은 진짜 `time` 모듈이라 전역이 오염된다). 깨끗한 프로세스에서
다시 재니 `[1.0, 2.0, 3.0]`. 자체 검증은 대역을 안 하므로 처음부터 통과하고 있었다.

**index.html 은 이번 회차에 변경 없음 — 아티팩트 재배포 안 함.**

---

## 2026-08-22 13:0x — 16회차 · 영역: 검색 질의문 조립(sweep_condition / search_min_dps)

**고침 · 조건 검색이 사용자가 저장한 옵션 조건을 통째로 덮어써서 '다른 집단'을 잼**
`sweep_condition` 이 `q["stats"] = [우리 조건]` 으로 **대입**하고 있었다. 기준 곡선을 뜨는
`search_min_dps` 는 `stats` 를 그대로 두므로, 사용자의 저장된 검색에 옵션 조건이 하나라도
있으면 두 곡선의 모집단이 달라진다. 실제 payload 를 찍어 재현:

    저장된 검색: 희귀 활 + explicit.stat_1509134228 >= 100
    기준 검색이 보낸 stats : [{... explicit.stat_1509134228 >= 100}]
    조건 검색이 보낸 stats : [{... explicit.stat_CRIT >= 2}]        <- 사용자 조건이 사라짐

"이 조건을 걸면 얼마 더 드나"는 **같은 모집단**에서 한 조건만 더한 값이어야 성립한다.
지금은 조건 곡선 쪽만 사용자 조건이 풀린 더 넓은 집단이라 비교가 성립하지 않고, 코드 곳곳이
경계하는 "조건 곡선이 기준보다 싸게 나오는 모순"(`optionFronts` 주석의 실측 708 > 679)이
바로 이 경로로 생길 수 있다. 활을 살 때 옵션 조건을 걸어 검색하는 건 아주 흔한 사용법이다.
→ 대입 대신 **기존 묶음에 덧붙이도록** 바꿨다. 재확인: 네 문턱값 전부
`['explicit.내것', 'explicit.우리것']` 두 묶음이 같이 나가고, `rarity: rare` 같은 다른 필터도
유지되며, 원본 질의문은 안 건드려 문턱값마다 조건이 쌓이지 않는다.

**검증 추가** — 대역한 `api_get` 으로 `sweep_condition` 의 실제 payload 를 받아, 사용자 조건과
우리 조건이 **둘 다** 들어있는지 / 다른 필터가 살아있는지 / 원본 질의문이 안 변했는지.

**남긴 관찰 1 (고치지 않음) — 저장된 검색의 DPS 상한이 사라진다**
`search_min_dps` 는 `["dps"] = {"min": lo}` 로 dict 를 통째로 바꾼다. 문턱값을 훑는 게 목적이니
`min` 을 덮는 건 의도대로지만, 사용자가 `dps {min:600, max:700}` 처럼 상한도 걸어뒀다면 그것까지
같이 지워진다. 다만 "곡선 전체를 뜬다"는 이 도구의 설계 의도와 어느 쪽이 맞는지 단정할 근거가
없어서 손대지 않았다.

**남긴 관찰 2 (검증 불가) — 필터 묶음의 `disabled` 플래그**
거래소 질의문은 필터 묶음에 `disabled` 를 실을 수 있다. `equipment_filters` 가 꺼져 있는 저장된
검색이라면 우리가 그 안에 넣는 `dps` 도 무시되어, **모든 밴드가 같은 매물을 돌려주고 구간 나누기가
조용히 무너진다.** 지금 수집분은 밴드별 total 이 10000/9307/8660... 으로 달라 이 상황이 아니었다.
플래그를 지우는 게 맞을 것 같지만 이 API 는 모르는 키에 `Unknown filter` 로 거절할 만큼 엄격해서
(과거 `weapon_filters`·top-level `sort` 로 실제로 거절당한 적 있음) 네트워크 없이 확정할 수 없다.
**수집 재개 시 저장된 검색에 `disabled` 가 실제로 오는지부터 확인할 것.**

**index.html 은 이번 회차에 변경 없음 — 아티팩트 재배포 안 함.**

---

## 2026-08-22 12:4x — 15회차 · 영역: 링크 검증 / CLI 기동 경로

**고침 · 수집기 쪽 진입점에만 "검색 링크인가" 검사가 빠져 있었다**
같은 검사가 두 곳에 따로 있었고 한쪽만 온전했다(11·13회차와 같은 유형).
`load_trade`(페이지의 /api/trade)는 경로에 `/trade2/search/` 가 있는지 확인하는데,
`resolve_search`(`--collect` 가 쓰는 쪽)는 호스트만 보고 **아무 경로나 통과**시켰다. 실측:

    resolve_search  https://poe.kakaogames.com/                      -> 네트워크로 나감
    resolve_search  https://poe.kakaogames.com/account/view-profile/x -> 네트워크로 나감
    resolve_search  https://poe.kakaogames.com/api/trade2/data/stats  -> 네트워크로 나감
    load_trade      위 셋 전부                                        -> 정상 차단

나가봐야 HTML 이 돌아오고 그걸 JSON 으로 읽다 죽는다. `--collect` 는 매시간 무인으로 돌고
URL 을 `last_url()` 로 DB 에서 되읽으므로, 한 번 잘못된 URL 이 들어가면 **매 회차 쓸모없는
요청을 거래소로 계속 쏘게 된다**(레이트 리밋만 태움).
→ `search_api_url()` 하나로 합쳐 두 진입점이 같은 검사를 쓰게 했다. 재확인: 정상 검색 링크만
네트워크로 나가고 나머지 6종은 요청 전에 막힌다. 페이지에서도 `/api/trade` 가 각각
`저장된 '검색' 링크여야 합니다` / `거래소 주소가 아닙니다` 로 정확히 갈라 답한다.

**고침 · 거래소가 HTML 을 보내면 TradeError 를 안 거치고 그대로 터진다**
`api_get` 은 HTTPError·URLError 만 감싸고 `json.loads` 실패는 안 감쌌다. 거래소는 Cloudflare
뒤에 있어서 점검·차단·로그인 화면이 HTML 로 오는 게 드문 일이 아니다(이 세션에서도 Cloudflare
챌린지 페이지를 실제로 만난 적이 있다). 그러면 한 번짜리 `--collect` 는 raw 트레이스백으로 죽고,
`--every` 루프는 `수집 오류: JSONDecodeError: Expecting value` 라는 알 수 없는 말만 남긴다.
→ 응답 앞부분을 붙여 `거래소가 JSON 이 아닌 응답을 보냈습니다(점검·차단·로그인 화면일 수
있습니다): <!DOCTYPE html><title>Just a moment...` 로 바꿨다. 가짜 응답으로 재현·확인.

**검증 추가** — `search_api_url` 이 잡 링크 6종(루트/프로필/데이터API/남의 호스트/접미사 위장/
ftp/빈 값)을 전부 막고, 정상 링크 2종은 통과시키며, `evil.com@poe.kakaogames.com` 같은
userinfo 위장은 진짜 호스트로 정규화하는지.

**문제 없던 것 — CLI 인자 파싱**
`arg()` 가 `sys.argv[:-1]` 로 멤버십을 보기 때문에 `--collect` 가 **맨 끝에 와도 IndexError 가
안 난다**(None 으로 떨어짐). `--collect --every 3600` 은 값이 `--every` 로 잡히지만 `main` 이
`startswith("--")` 로 걸러 `last_url()` 로 넘어간다. `--every` 는 `max(600, ...)` 로 10분
하한이 걸려 있어 실수로 0 을 줘도 폭주하지 않는다.

**보안 회귀 없음(재확인)** `snapshots.db`/`serve.py` 는 GET·HEAD 둘 다 404,
`Host: evil.com` 은 403. 2회차에 넣은 방어가 그대로 살아 있다.

**index.html 은 이번 회차에 변경 없음 — 아티팩트 재배포 안 함.**

---

## 2026-08-22 12:2x — 14회차 · 영역: 매물 표 / 삭제 / 계단 · **이번 회차 발견 없음**

억지로 바꾸지 않았다. 코드 변경 0, 아티팩트 재배포 없음. 본 것과 확인 방법:

**삭제가 엉뚱한 활을 지우지 않는지 (집합 비교로 엄밀히)**
표는 `가격/DPS` 로 정렬되고 조건·희귀도로 걸러지므로, 화면의 줄 번호와 `bows` 의 실제
위치가 다르다. 여기서 인덱스를 잘못 쓰면 옆 활이 지워진다. 활 116개에 고유 표식을 붙이고
세 상황(조건 켠 표의 첫 줄 / 4번째 줄 / 조건 없는 표의 마지막 줄)에서 삭제해봤다.

    표에 보인 이름 == bows[data-i] 의 이름   3/3 일치
    사라진 개수                              항상 정확히 1개
    사라진 것이 기대한 그 활인가             3/3 일치

`partition()` 이 `bows.map((b, i) => ...)` 로 **원본 인덱스**를 들고 다니게 만들어 둔 덕분에,
정렬·필터를 거쳐도 `data-i` 가 계속 맞는다. 결함 아님.

**계단(최전선 구간) 18개 조건 전수**
DPS 증가분이 음수인 칸 0건, 비용에 NaN/Infinity/음수 0건, `무릎` 강조가 둘 이상 붙은 조건 0건.
구조적으로도 최전선은 DPS 오름차순이고 가격이 뒤로 갈수록 반드시 커지므로(아니면 앞 점이
최전선에서 밀려난다) 구간 비용이 음수가 될 수 없다.

**극단적으로 작은 데이터셋** 0개 / 1개 / 2개 / 셋 다 동일값 / 같은 가격 다른 DPS /
같은 DPS 다른 가격 — 전부 예외 0건. 차트는 2개 미만에서 안내문으로, 계단은 최전선 2점
미만에서 안내문으로, 예산 카드는 비교할 조건이 없으면 숨김으로 정확히 빠진다.

**모바일 폭(375px)** 가로 페이지 스크롤 없음(문서폭 = 화면폭 375). 넘치는 요소 1,333개는
전부 가로 스크롤 컨테이너 안이라 정상이다 — 컨테이너 없이 넘친 요소 0개.

**11회차 수정 역검증(내가 직전에 만진 코드 다시 보기)**
정규식에 `^` 앵커를 붙였으니 진짜 무기 로컬 옵션이 새어나갈 수 있다 — 전체 4,131개로 대조:

    물리 피해 #% 증가 736/736 · 공격 속도 #% 증가 348/348 · 피해 #~# 추가 5종 911/911
    "셈" 으로 잡힌 것 중 로컬 목록 밖의 것: 없음
    앞뒤 공백이 있는 옵션 문자열: 0개 (앵커가 깨질 입력이 없다)

누락도 과잉도 없음.

---

## 2026-08-22 12:0x — 13회차 · 영역: pick_conditions(수집기의 조건 선택)

**고침 · 페널티 옵션(`투사체 사거리 #% 감소`)을 조건으로 골라 요청을 낭비하고, 정반대 집단을 잼**
실제 수집분 473개로 `pick_conditions` 를 돌려보니 빈도 2순위(247건)로 이게 뽑혔다.
두 가지가 동시에 잘못돼 있었다.

① **페이지와 목록이 어긋남.** `index.html` 의 `JUNK_MOD` 에는 `투사체 사거리` 가 있는데
`serve.py` 의 `JUNK_WORDS` 에는 없었다. 주석에는 "수집기와 같은 기준"이라고 적혀 있었지만
실제로는 아니었다 — 11회차의 `COUNTED`/`COUNTED_KR` 와 같은 유형의 이중 정의 어긋남.

② **부호가 사라져 반대 집단을 검색한다.** 카탈로그는 음수 옵션을 "증가" 항목으로만 싣고
`resolve_stat` 이 `감소 -> 증가` 로 뒤집어 찾는데, `mod_val` 은 부호를 버린다. 그래서

    관측    "투사체 사거리 50% 감소"  (사거리가 '줄어든' 활)
    검색    {id: <증가 stat>, min: 50}  = 사거리가 '50% 이상 늘어난' 활

즉 관측한 것과 **정반대 활**의 가격 곡선을 떠서 "이 옵션에 얼마를 더 내나"로 보여준다.
→ `is_useful_stat` 이 `감소`/`reduced` 가 들어간 옵션을 아예 빼도록 했다(부호가 뒤집히는
구조라 조건이 될 수 없다). `JUNK_WORDS` 에도 투사체 사거리를 넣어 페이지와 목록을 맞췄다.
고친 뒤 빈도 두 자리는 `반려수의 공격 속도`(239건, 11회차에 되살린 옵션)와
`처치한 적 하나당 생명력 획득`(152건) — 둘 다 구매자가 실제로 값을 치르는 옵션으로 바뀌었다.

**고침 · docstring 이 코드에 없는 동작을 설명함**
"문턱값은 표본에 그 옵션이 보이면 관측값 상위 25% 지점" 이라고 적혀 있지만, 우선 목록은
언제나 코드에 박힌 `steps` 를 쓰고 상위 25% 는 빈도로 채우는 쪽에만 적용된다.
다음 회차가 이걸 근거로 잘못 판단하지 않도록 실제 동작대로 고쳤다.

**검증 추가** — `is_useful_stat` 8종: 감소 계열(한/영)·잡옵션·implicit 은 빼고,
정상 옵션과 `민첩 10당 공격 속도 #% 증가`(부분 일치로 빼면 안 되는 것)는 통과하는지.

**헛짚은 것(기록)** 처음 돌렸을 때 우선 조건 8개의 `n` 이 전부 0 이라 집계가 깨진 줄 알았다.
원인은 내가 만든 **가짜 카탈로그**가 우선 옵션에 엉뚱한 stat id 를 붙인 것이었다 —
실제 id 를 넣으니 n=64/50/18/121/116 으로 정상. `latest.json` 의 값과도 맞는다. 결함 아님.

**index.html 은 이번 회차에 변경 없음 — 아티팩트 재배포 안 함.**

---

## 2026-08-22 11:4x — 12회차 · 영역: /api/trade 요청 흐름(불러오기 버튼)

**고침 · "교체"로 불러오다 결과가 0개면 기존 목록이 통째로 사라진다 (복구 불가)**
순서가 거꾸로였다 — **먼저 지우고 나서** 새 데이터를 붙였다:

    if (d.error) { ...; return; }
    if (mode === 'replace') { bows = []; reqs = {}; }   // <- 여기서 이미 비움
    bows = bows.concat(stamped(d.bows));               // <- 여기서 0개거나 던짐

`serve.py` 는 검색이 성공했어도 실거래 화폐 넷 밖이거나 DPS 가 없는 매물을 전부 버리므로
`{bows: [], total: 2758}` 이 정상 응답으로 온다(9회차 참고 — 한 스냅샷은 120개 중 51개가
규격 밖 화폐였다). 그러면 지운 뒤 붙일 게 없어서 목록이 빈 채로 `save()` 까지 간다.
브라우저 실측(스냅샷 116개 + 손으로 넣은 활 1개):

    전   매물 117 · localStorage 117 · 표 117행
    후   매물   0 · localStorage   0 · 표   0행
    안내 "0개 불러옴 (검색 결과 2758개, 20개는 가격이나 DPS가 없어 제외)"

안내문이 **성공한 것처럼** 읽히는 게 특히 나쁘다. 스냅샷 매물은 새로고침하면 돌아오지만
**손으로 등록한 활은 복구할 방법이 없다.** 응답 모양이 이상할 때(`{}`, `bows` 가 문자열)도
같은 자리에서 던져서 결과가 똑같았다 — "지우고 나서 실패".
→ `freshRows(d)` 로 먼저 쓸 수 있는 매물만 꺼내고, **0개면 아무것도 안 지우고 돌아간다.**
재확인: 세 경우 모두 117개가 상태·localStorage·화면 전부에서 그대로 유지되고,
정상 응답에서는 교체가 예전처럼 동작한다.

**검증 추가** — `freshRows` 가 `{}` / `{bows:'헐'}` / `null` / `[null,3,'x']` 에서 빈 배열을
돌려주고(던지지 않고), 정상 응답에선 개수와 시각 도장이 붙는지.

**문제 없던 것 — 나머지 불러오기 흐름**
빈 URL 은 요청 전에 막고, `limit` 9999 는 서버로 나가기 전에 100 으로 죄어진다
(실제 요청 URL 확인: `...&limit=100`). 네트워크 실패는 잡아서 안내로 바꾸고, 버튼 잠금은
`finally` 라 어느 경로로 끝나도 풀린다. 실행기가 없으면 `/api/ping` 실패로 버튼이
"실행기 필요"로 잠긴 채 시작한다 — 이 상태에선 클릭 자체가 안 먹는다.
콘솔 오류·미처리 거부 0건.

**검증 방법 메모** 이번 회차는 `python -m http.server` 로는 부족했다(`/api/trade`·`/api/ping`
이 없어 버튼이 잠긴 채라 흐름을 못 탐). `serve.py` 의 진짜 `Handler` 를 `--collect` 없이
별도 포트로 띄우되 `webbrowser.open` 은 부르지 않도록 직접 기동해서 확인했다.
거래소로 나가는 요청은 `fetch` 를 대역해 전부 차단.

---

## 2026-08-22 11:2x — 11회차 · 영역: 옵션 분류(DPS 안/밖 판정)

**고침 · "반려수의 공격 속도" 를 무기 공속으로 착각해 조건 목록에서 통째로 숨김**
`COUNTED` 의 한국어 패턴이 `공격 속도.*증가` 로 느슨해서, 동료 펫 옵션인
`[Companion|반려수]의 [Attack|공격] 속도 14% 증가` 까지 "이미 DPS 에 반영됨"으로 잡았다.
**수집한 473개 중 241개(51%)가 이 옵션을 갖고 있다** — 물리 피해 증가 다음으로 흔한데
조건 후보에서 완전히 빠져 있었다.

무기 DPS 와 무관함을 데이터로 확인(추정 아님). 무기 자체 공속만으로 베이스 aps 를 역산:

    무기 공속만으로 베이스 aps 가 맞아떨어짐   217개
    반려수 공속까지 포함해야 맞는 경우          0개
    (공속 옵션이 없는 활의 aps 는 1.10/1.15/1.20/1.25 로 딱 떨어짐 = 베이스값)

같은 느슨함이 `피해.*추가` 에도 있어서 `피해의 #%를 추가 카오스 피해로 획득`,
`피해의 #%를 모든 원소의 추가 피해로 획득` 도 잘못 삼키고 있었다(둘 다 무기 로컬 옵션이 아님).
→ 한국어 패턴을 줄 맨 앞에 붙여 잡고(`^공격 속도 [\d.]+% 증가`, `^물리 피해 [\d.]+% 증가`),
"추가"는 `피해 \d+~\d+ 추가` 범위 표기를 요구하도록 좁혔다. **`serve.py` 의 `COUNTED_KR` 도
같은 기준이라 함께 고쳤다** — 한쪽만 고치면 수집기와 페이지의 판정이 갈린다.

실제 4,131개 옵션 문자열 재분류:

    "DPS 로 이미 셈"   12종 -> 7종  (진짜 무기 로컬 옵션만 남음)
    "조건 후보"        22종 -> 26종 (반려수 공속 239개가 1위로 올라옴)

브라우저 확인: 조건 17행 -> 18행, 새 조건을 고르면 매물 77개가 걸리고
**"이 조건을 걸면 +3.22 ex (약 +5%) 더 듭니다"** — 헌트리스 활 구매자에게 실제로 쓸모 있는
답(사실상 공짜 옵션)이 그동안 아예 안 보이고 있었다. 콘솔 오류 0, NaN 0.

**검증 추가** — 양쪽 파일에 같은 6종: 반려수 공속 / 접근해 있는 반려수 / "추가 ~로 획득" 은
DPS 밖으로, 무기 자체 물리 피해·공속·"화염 피해 10~16 추가" 는 DPS 안으로 판정하는지.

**문제 없던 것 — 잡옵션 필터**
사용자가 빼달라고 한 셋(`능력치 요구사항 #% 감소`, `민첩 #`, `모든 능력치 #`)은 전부 정상 제외
중이고, `시야 반경`·`투사체 사거리`·`결속됨:`(룬) 계열도 의도대로 걸린다. 이번 수정으로
`결속됨: 피해의 #%를 추가 물리 피해로 획득` 이 "이미 셈"에서 빠졌지만 `결속됨:` 접두사가
받아내 여전히 잡옵션이다 — 분류가 두 그물 다 통과하는 구멍은 없다.

---

## 2026-08-22 11:0x — 10회차 · 영역: 예산표 / 삼각 환율 계산(serve.py)

**고침 · 우회 환율이 0 까지 깎여 "DPS 501 활을 0.0000 ex 에 산다"가 최전선 맨 앞에 뜸**
`solve_rates()` 의 우회 단계는 경로마다 최솟값을 취한다. 관측이 서로 안 맞으면(차익 순환)
값이 순환을 돌 때마다 깎이는데, `MAX_HOPS = 3` 이 막는 건 **횟수뿐이라 3회면 이미 0 에 닿는다**.
주석은 "3홉이면 안전"이라고 적혀 있었지만 실측은 그렇지 않았다:

    입력: divine 직접 400 + divine->chaos 0.1 + chaos->annul 0.1 + annul->divine 0.1 ...
    결과: chaos 0.0000 ex,  annul 0.0004 ex          <- 지어낸 값

그 값이 `latest.json` 으로 나가면 페이지가 그대로 믿는다. 브라우저에서 `chaos` 환율만
4e-7 로 바꿔 재현:

    정상    최전선 맨 앞 = 명예의 공성 광신도 활 · 346.2 DPS · 1.00 ex
    깨진 뒤 최전선 맨 앞 = 침울한 화살깃 절멸자 활 · 501.1 DPS · 0.0000 ex   <- 공짜 매물

가격이 0 이 아니라 **아주 작은 양수**라 `p > 0` 필터를 통과한다 — 제외 문구도 안 뜨고
곡선 전체가 그 한 점에 끌려간다. 매물 두 개가 곡선을 통째로 망가뜨리는 경로였다.
→ 두 군데를 막았다. ①`solve_rates` 가 1 미만·비유한 값을 내보내지 않고 그 화폐를 빼버린다
(엑잘이 이 넷 중 가장 작은 단위라 1 미만은 관측이 깨졌다는 뜻). ②페이지 `rateOf()` 도
스냅샷 환율이 유한한 1 이상이 아니면 기본값으로 물러난다 — 옛 스냅샷이 남아 있을 수 있어서.
재확인: 4e-7 을 먹여도 `rateOf('chaos')` = 65(기본값), 근거 칸도 "기본값", 최전선 정상 복귀.

**검증 추가** — serve.py: 차익 순환이면 해당 화폐가 결과에서 빠지는지, 직접 매물이 0 이면
빠지는지, 정상 우회(`divine 400` + `divine->annul 0.4` → `annul 160`)는 그대로인지.
index.html: `okRate` 가 4e-7/0/음수/NaN/Infinity/문자열/undefined 를 전부 막고 1·300 은 통과하는지.

**문제 없던 것 — 예산표 전수 검사**
예산 슬라이더를 0~1000 까지 50 간격으로 21곳 훑으며 17행을 매번 대조했다.
기준 DPS 단조 증가, 조건행이 기준행을 넘는 모순 0건, `gap` 칸과 실제 차이 불일치 0건,
콘솔 오류 0건. 끝점 `posToBudget(1000)` 이 `hi` 에 정확히 닿는지 소수 lo 6조합으로 확인 —
부동소수 오차 0(끝의 최고가 매물이 예산에서 밀려나는 일이 없다).

**남긴 관찰(고치지 않음)** 지금 스냅샷의 `chaos` 환율이 **1 exalted**(직접 매물 1건)다.
시세로는 말이 안 되지만 수집기는 관측된 유일한 매물을 정직하게 옮겼을 뿐이고, 카오스 표기
매물이 2개뿐이라 영향이 미미하다. 얇은 시장의 표본 문제라 코드로 판정할 근거가 없어 둔다.

---

## 2026-08-22 10:4x — 9회차 · 영역: serve.py normalize() / 수집 집계

**고침 · "제외 N개"가 무조건 0 — 버려진 매물을 아무도 안 세고 있었다**
`load_banded()` 의 마지막 줄이 `return out, total, 0, base, league` 였다. 세 번째가 skipped 인데
**상수 0** 이라, 매 수집마다 찍히는 `"81개 저장 (검색 결과 10000, 제외 0)"` 의 마지막 값이
집계가 아니라 고정 문구였다(지난 수집 로그 전부 0). `latest.json` 의 `skipped` 도 같은 값.

버려지는 게 왜 중요하냐면 — `normalize()` 는 **실거래 화폐 넷 밖**이거나 GGG 가 DPS 를 안 준
매물을 조용히 버리는데, 그게 대부분 **싼 매물**이다. DB 에 남은 증거:

    스냅샷 #3 (03:03, 화폐 필터 도입 전)  120개 중 transmute 46 + aug 5 = 51개 (42%)
    스냅샷 #4 이후                        전부 규격 내 (필터가 실제로 동작 중)

즉 지금 기준으로 #3 을 다시 뜨면 120개 중 51개가 사라지는데 화면엔 "제외 0" 이 찍힌다.
싼 쪽이 통째로 빠지면 곡선 아래쪽이 들려서 "바닥 시세"가 실제보다 비싸 보인다.
→ `skipped += len(ids) - len(rows)` 로 밴드마다 누적. 같은 계산을 `load_trade()` 는 이미
제대로 하고 있었다(`len(wanted) - len(out)`) — 두 경로 중 한쪽만 맞춰져 있던 것.

**검증 추가** — `resolve_search`/`search_min_dps`/`fetch_ids`/`throttle` 을 대역해서
셋을 요청했는데 하나만 돌아오면 skipped==2, 전부 돌아오면 0 인지 확인(네트워크 안 탐).

**문제 없던 것 — normalize() 경계 13종**
한국어/영문 응답, 가격 없음, 금액 0, 소수 금액(0.5 div), 규격 밖 화폐, extended 없음,
edps 만 있음, properties 통째로 없음, values 빈 배열, 이름 없이 typeLine 만, 빈 dict —
**예외 0건**, 버릴 것은 버리고 살릴 것은 살린다. 한국어 `초당 공격 횟수`/`치명타 명중 확률` 과
영문 `Attacks per Second`/`Critical Hit Chance` 둘 다 정상 추출.

**실제 수집분 473행 역검증** aps 0 인 행 0개, crit 0 인 행 0개, 이름 없음 0개,
pdps·edps 둘 다 0 인 행 0개, 등급은 전부 Rare. aps 1.10~1.43 / crit 5.00~9.99 로
활의 실제 분포와 맞는다 — 파싱이 조용히 어긋난 열이 없다는 뜻.

**남긴 관찰(고치지 않음)** GGG 가 `pdps`/`edps` 를 **0 으로 명시**해 보내면 `is None` 검사를
통과해 수집된다. 페이지가 `d > 0` 으로 걸러 "DPS나 가격이 0인 N개 제외"로 잡히므로 조용히
사라지진 않지만, 수집 단계에선 쓸모없는 행이 하나 는다. 실제 데이터에 그런 행은 0개라 방치.

**index.html 은 이번 회차에 변경 없음 — 아티팩트 재배포 안 함.**

---

## 2026-08-22 10:2x — 8회차 · 영역: 곡선 렌더링 / premium 계산 경계

**고침 · 로그축 맨 아래 눈금이 "0.00" 으로 찍힘 (로그축엔 0 이 있을 수 없다)**
자릿수 규칙이 두 곳에 따로 있었다. `money()` 는 값이 작아질수록 소수를 늘리는데
(`>=0.1 ? 3 : 4`), 축 눈금은 자기 규칙을 인라인으로 갖고 있었고 **소수 둘에서 멈췄다**.
그래서 큰 단위를 고르면 아래쪽 눈금이 전부 0 으로 뭉갰다. 브라우저 실측:

    로그 · 디바인   0.00  0.03  0.25  2.5  25.0  250     <- 1 ex 자리가 "0.00"
    로그 · 소멸     0.00  0.03  0.25  2.5  25.1  251
    선형 · 아무거나  "0.00" (원점인데 소수 둘)

사용자가 단위 선택을 요청한 이유가 "숫자가 크면 보기 불편해서"인데, 정작 큰 단위를 고르면
싼 매물 구간이 통째로 "0.00" 이 되어 반대 방향으로 같은 문제를 만들고 있었다.
→ 자릿수 규칙을 `decimalsFor(v)` 한 곳으로 모으고 `money()` 와 축 눈금이 같이 쓰게 했다.
원점(선형축 0)만 `tickLabel` 에서 그냥 `"0"` 으로. 재확인:

    로그 · 디바인   0.0025  0.0250  0.250  2.50  25.0  250
    선형           "0"

`getBBox()` 로 잘림도 확인 — 가장 긴 눈금("162,470")이 x=4 에서 시작해 왼쪽 여백(58) 안이다.
표의 가격 칸은 값이 그대로다(`money` 는 규칙을 밖으로 꺼냈을 뿐 동작 동일: "1.00 ex", "0.0289 ex").

**검증 추가** — `decimalsFor` 5구간과 `tickLabel(0)==='0'`,
그리고 `+tickLabel(0.0025) !== 0`(작은 값이 0 으로 뭉개지지 않는지).

**문제 없던 것 — 조건 17개 전수 렌더링**
조건을 하나씩 전부 골라 곡선·설명·SVG 를 훑었다. NaN/Infinity/undefined 0건, 콘솔 오류 0건,
음수 프리미엄 0건(부분집합이 기준보다 싸지는 모순 없음). 점이 1개뿐인 조건 3개는 3회차에 넣은
"1개뿐입니다" 경로로 정확히 빠진다. 단위 5종 × 로그 on/off 10조합에서 중복 눈금도 없다.

**헛짚은 것(기록)** 처음엔 "조건을 바꿔도 곡선이 안 변한다"고 봤는데, `buildConds()` 가
`#filters` 를 통째로 다시 그려서 **내가 미리 잡아둔 라디오 참조가 DOM 에서 떨어져 나간** 탓이었다
(분리된 요소에 dispatch 하면 위임 핸들러까지 안 간다). 매번 다시 질의하니 정상. 사이트 결함 아님.
6회차에 "조건 UI 는 innerHTML 재생성 뒤에도 멀쩡하다"고 확인한 것과 일치한다.

**참고** 차트가 조건과 무관하게 항상 점 116개를 그리는 건 의도된 동작이다 — 기준 매물 전부를
그리되 조건에 안 맞는 점은 `opacity 0.28` 로 흐린다(`chart()` 의 `dim`).

---

## 2026-08-22 10:0x — 7회차 · 영역: 스냅샷 저장 경로 / 데이터 보존

**고침 · 수집이 도중에 실패하면 지난 스냅샷이 반토막 난 채 남는다 (데이터 유실)**
`open(LATEST, "w")` 는 여는 순간 파일을 비우고, `json.dump` 는 거기에 흘려 쓴다. 그래서 쓰기
도중 무슨 일이 생기면 **직전에 잘 있던 스냅샷이 복구 불가로 깨진다**. 사본으로 재현:

    원본        77720 바이트 / 매물 116개
    매물 하나가 직렬화 실패
    남은 것     38931 바이트 (50%) -> JSONDecodeError, 페이지가 아예 못 읽음

`--collect --every` 는 매시간 무인으로 돌고 종료 방법이 Ctrl+C 라, 77KB 를 쓰는 창에 인터럽트가
걸리는 경로가 상시 열려 있었다. 직렬화 오류·정전도 같은 결과다.
→ `write_latest()` 로 분리해 **옆에 `.tmp` 로 쓰고 성공했을 때만 `os.replace()`** 로 갈아끼운다.
같은 폴더 안이라 원자적이고, 실패하면 원본이 손도 안 탄 채 남는다.

**검증 추가** — 임시 폴더에서 ①정상 쓰기 ②중간에 실패하는 쓰기 ③원본이 바이트 단위로 그대로인지
④그 뒤 정상 쓰기가 갈아끼우는지. 실제 `latest.json` 은 건드리지 않는다(`path` 인자로 주입).

**문제 없던 것 — DB 쓰기와 경로**
`DB`/`LATEST` 는 `__file__` 기준 절대 경로라 어느 폴더에서 실행해도 같은 곳을 본다.
snapshots/bows 삽입은 `with db()` 트랜잭션 안이라 실패하면 통째로 롤백된다(부분 스냅샷이 안 남음).

**문제 없던 것 — 깨진 latest.json 을 페이지가 만났을 때**
반토막 사본을 로컬 서버로 먹여봤다. `loadSnapshot()` 의 `try` 가 함수 전체를 감싸고 있어
`r.json()` 이 던지는 시점엔 아직 아무것도 대입 전이다 — 상태가 반쯤 갱신되는 일 없이
"매물 없음" 빈 화면으로 안전하게 떨어지고 콘솔 오류 0, 자체검증 통과.

**문제 없던 것 — 시점 혼재 경고와 "최근 것만 남기기"**
10일 전 2개 + 방금 1개 + 시점 불명 1개를 심으니 경고가 정확히 뜨고("시점 불명 1개, 8월 12일~
8월 22일"), 버튼이 innerHTML 재생성 뒤에도 동작(위임 처리)해 4개 → 1개로 정리됐다.

**헛짚은 것(기록)** 물리만 있는 활이 `total` 기준에서 조용히 빠지는 걸 발견했는데, 원인은
내 시험 데이터가 `emin`/`emax` 를 뺀 것이었다. 실제 경로는 전부 숫자를 채운다 —
직접 등록은 `+value || 0`, 거래소·스냅샷 매물은 `pdps`/`edps` 를 그대로 받는다. 결함 아님.

**index.html 은 이번 회차에 변경 없음 — 아티팩트 재배포 안 함.**

---

## 2026-08-22 09:4x — 6회차 · 영역: 조건 필터링 / 조건 UI 의 DOM 생명주기

**고침 · 같은 옵션이 "측정"과 "표본" 두 줄로 나란히 뜸 (1회차 수정이 옛 수집본엔 안 통함)**
1회차에 수집기가 `key` 를 내보내도록 고쳤지만, 페이지는 `c.key || c.label` 로 **키가 없으면
라벨을 그대로 열쇠 취급**한다. 그런데 지금 서비스 중인 `latest.json` 은 그 수정 이전 수집본이라
conds 10개 전부 `key` 필드가 없다(브라우저에서 실측). 결과:

    현재      열쇠로 쓰는 값이 로컬 옵션과 일치: 10개 중 1개
    조건 목록  20행 — 치명타 확률 / 치명타 피해 보너스 / 정확도가 각각 두 줄

즉 1회차의 "21→17행" 은 그 세션에서 갓 수집한 데이터에서만 성립했고, 사용자가 실제로 보는
화면에서는 그대로 되살아나 있었다. 수집을 멈춘 상태라 데이터 쪽으로는 못 고친다.
→ 페이지가 스스로 라벨에서 열쇠를 만들도록 `condKey(c)` 추가. 라벨이 `"<문구> <값> 이상"`
꼴이라 뒤의 "이상"만 떼고 `modKey()` 를 태우면 일반 옵션 문구와 같아진다. 10개 중 6개 일치.
브라우저 재확인: **조건 목록 20행 → 17행**, 예산표에서도 같은 3행이 사라짐.
남은 불일치 2개는 원인이 따로다 — `생명력 흡수 5% 이상` 은 1회차에 확인한 잘못된 문구(옛
수집본이라 그대로), `민첩 #` 은 페이지가 잡옵션으로 거르는 항목. 둘 다 데이터 문제.

**고침 · 값이 하나뿐인 옵션의 문턱 막대가 끌어도 아무 일도 안 함**
`bar()` 가 `vals.length < 2` 만 보고 `min === max` 는 안 봤다. `활 공격 시 화살 #개 추가 발사`
는 관측 7개가 전부 1이라, 막대를 0 에 둬도 최대치에 둬도 걸리는 활이 **14개로 동일**하다(실측).
→ `min === max` 면 막대를 안 만든다. 정상 옵션(마나 흡수 5.4~8.85)은 그대로 24개→2개로 반응함을 확인.

**검증 추가** — `condKey` 4종(이상 라벨/정확도/이미 열쇠/명시 key 우선)과
`bar()` 가 값 하나뿐이면 빈 문자열을 돌려주는지.

**문제 없던 것 — 조건 UI 의 DOM 생명주기**
표본 조건을 고른 채 기준 설정(총 DPS → 원소 DPS)을 바꿔 `refresh()` → `buildConds()` 로
목록을 통째로 다시 그려도 선택·막대·`pickedCond` 가 전부 유지된다. 문턱 막대를 최대치, 최대치의
2배, 0 으로 연달아 끌어도 콘솔 오류 0, 차트 정상. 조건이 1점으로 줄면 3회차에 넣은
"1개뿐입니다" 문구로 정확히 빠진다.

---

## 2026-08-22 09:2x — 5회차 · 영역: 스로틀 / 레이트 리밋

**고침 · 요청이 실패하면 같은 창을 반복해서 통째로 다시 잠 (수집이 한 시간씩 늘어남)**
레이트 리밋 헤더는 "그 응답 시점"의 사용량인데, `throttle()` 이 그 뒤로 흐른 시간을 안 뺐다.
헤더는 응답이 성공해야만 갱신된다 — 그런데 `pair_rate` 는 `TradeError` 를 삼키고 넘어가므로,
교환 요청이 실패하기 시작하면 낡은 "한도 직전" 상태가 그대로 남아 매 요청마다 창 전체를 다시 잔다.
가짜 시계로 재현(교환 15초 창, 4/5 사용):

    고치기 전   15초, 15초, 15초, 15초   ← 요청은 한 번도 안 나갔는데 계속 잠
    고친 뒤     15초, 10초, 10초, 10초   ← 첫 회만 창, 이후는 평소 페이싱 간격

최악은 300초 창이다. 화폐 12쌍을 도는 동안 계속 실패하면 **60분을 자면서 아무것도 안 한다**
(고친 뒤 6.8분). 코드 주석이 경계하던 "예상 13분이 25분이 된" 현상의 정체가 이것으로 보인다.
→ 헤더에 관측 시각(`at`)을 같이 저장하고, `panic` 을 `window` 가 아니라 `window - age` 로 계산.
창이 이미 지났으면 아예 안 잔다. 하루 예산(6시간 창) 소진 시 접는 동작은 그대로 유지됨을 확인.

**검증 추가** — 가짜 시계로 ①연속 4회가 `[15,10,10,10]` 인지 ②창이 절반 지났으면 남은 5초만 자는지
③6시간 예산이 바닥나면 자지 않고 `TradeError` 로 접는지, 세 가지를 self-test 에 넣음.

**문제 없던 것 — 나머지 스로틀 경로**
`bucket_of` 는 세 엔드포인트를 경로로 정확히 가른다. 규칙/상태 문자열이 깨졌거나 개수가 안 맞아도
`ValueError`/`zip` 으로 조용히 건너뛴다. `MAX_WAIT` 초과 시 자지 않고 접는 것, 429 를 한 번만
재시도하고 `Retry-After` 가 길면 접는 것 모두 의도대로 동작.

**미해결 항목 재조사 — 문턱값은 문제가 아니었다 (변경하지 않음)**
"정확도 +100 이 시장 대비 너무 높다"는 진단을 snapshots.db 관측치로 검증했더니 반대였다.

    정확도 #   n=121   중앙값 123   100 이상이 55%      <- 문턱 100 은 사실상 중앙값
    조건 검색 결과: 다른 조건은 DPS 문턱 4곳 모두 성공, 정확도만 2곳

문턱을 낮춰도 안 늘어난다. 로컬에서 55%가 만족하는데 검색은 거의 0건이라는 건 문턱이 아니라
stat id 가 다른 것을 가리킨다는 뜻 — 1회차 가설(전역 정확도 `stat_803737631` vs 로컬
`정확도 #(특정)` `stat_691932474`)과 일치한다. 확인에 카탈로그 조회가 필요해 이번에도 보류.

**생명력 흡수** 1회차에 고친 문구가 맞다(`물리 공격 피해의 #%를 생명력으로 흡수`, n=116).
문턱 6은 그 옵션을 가진 활 중 99%가 통과하지만, **전체 473개 중 116개만 이 옵션을 갖는다** —
"쓸 만한 흡수가 붙어 있는가"를 묻는 조건으로 정상이므로 건드리지 않음.
(통과율을 전체 대비로 착각해 "무의미한 필터"로 오판할 뻔했다. 분모는 그 옵션 보유 매물이다.)

**index.html 은 이번 회차에 변경 없음 — 아티팩트 재배포 안 함.**

---

## 2026-08-22 09:0x — 4회차 · 영역: 파서 / 직접 등록 / 저장 상태

**고침 · 저장값이 깨지면 페이지가 부팅 중 죽음**
`try/catch` 가 JSON 파싱 오류만 잡고 "모양"은 안 봤다. `localStorage.poe2bows` 가 `"123"` 이면
`JSON.parse` 는 성공해서 `bows` 가 숫자가 되고, 곧바로 `bows.map is not a function` 으로
렌더가 통째로 죽는다. 실측: 표 0행, 예산표 0행, 차트 없음 — 그리고 저장값을 손으로 지우기
전까지 영구 고착(다시 열어도 같은 값을 읽으므로).
같은 유형으로 `poe2rates` 가 배열/숫자면 `cur in rates` 가 TypeError 를 던진다.
→ `readStore(key, fallback, ok)` 로 파싱 후 모양까지 확인하도록 수정.
bows 는 배열+객체 원소만, rates 는 순수 객체만, unit 은 아는 값이 아니면 auto 로 되돌림.
재검증: `"123"` / `"문자열"` / `[1,2,3]` / `<script>` 를 심고 새로고침 → 매물 116, 차트 정상,
환율칸 3개, 자체검증 통과.

**검증 추가** — 손상값 5종에 대해 readStore 가 항상 올바른 타입을 돌려주는지 self-test 에 넣음.

**문제 없던 것 — XSS 실제 시도**
`<img src=x onerror=...>` 를 아이템 이름과 옵션 문구에 넣어 표/조건목록/예산표까지 렌더시킴.
스크립트 실행 0, img/svg 요소 생성 없음, 페이로드가 5곳에서 이스케이프됨. esc() 가 제 역할을 함.

**문제 없던 것 — 파서 경계**
빈 문자열/공백만/구분선 없음/CRLF/원소 여러 줄(합산 정상)/숫자 아닌 값/음수 전부 예외 없이 처리.
이름에 콜론이 있으면 베이스 타입을 이름으로 잡는데, 이는 "라벨: 값" 을 걸러내는 휴리스틱의
알려진 한계이지 결함이 아니다.

**남긴 관찰(고치지 않음)** 직접 등록에서 음수 가격·음수 피해가 통과된다. 다만 이후
`d > 0 && p > 0` 필터가 걸러내고 "DPS나 가격이 0인 N개 제외"로 집계되어 조용히 사라지진 않는다.
크래시나 오계산이 아니라 이번 회차에는 손대지 않음.

---

## 2026-08-22 08:4x — 3회차 · 영역: 환율 환산 / 예산 계산 경계

**고침 · 같은 값 매물이 중복으로 남아 "점 하나"를 곡선으로 단정**
최전선은 (DPS, 가격)이 완전히 같은 점을 서로 밀어내지 못해 여럿 남긴다. 실측 중복 현황:

    기준 최전선            23점 중 6점 중복 (781.1@590800 은 3번)
    치명타 피해 +30% 이상    4점 중 3점 중복 -> 실제로는 점 하나
    정확도 +100 이상        2점 중 1점 중복 -> 실제로는 점 하나

`premium()` 이 `f.length < 2` 로 판단해서, 측정이 사실상 한 번뿐인 조건에도
"이 조건을 걸면 +N ex 더 듭니다"라고 곡선인 척 단정했다. 일부러 넣어둔 "1개뿐입니다" 경로가 우회됨.
→ `distinctPts()` 로 서로 다른 점의 개수를 세어 판단하도록 수정.
frontier() 자체는 안 건드렸다 — 표에서는 동점 매물 전부에 "최전선" 딱지가 붙는 게 맞기 때문.
재검증: 치명타 피해 +30%/정확도 +100 은 "1개뿐" 문구로, 치명타 확률 +2%(서로 다른 4점)는
기존 곡선 문구 그대로.

**문제 없던 것**
`posToBudget` 은 lo==0 이면 NaN 을 내지만 `budgetRange` 가 `p > 0` 으로 걸러 도달 불가.
`money`/`perDps` 는 0·음수·Infinity·NaN 에서 정상 처리. 모르는 화폐는 rateOf 0 -> 가격 0 ->
"DPS나 가격이 0인 N개 제외"로 집계되어 조용히 사라지지 않음. pickUnit 은 환율을 전부 몰라도
exalted 로 안전하게 떨어짐.

**남긴 관찰(고치지 않음)** 모든 매물 가격이 같으면(리그 초 실제로 그랬다) lo==hi 라
예산 슬라이더가 아무 반응을 안 한다. 계산은 정확하나 슬라이더가 죽은 것처럼 보인다.
정확성 결함은 아니라 이번 회차에는 손대지 않음.

---

## 2026-08-22 08:2x — 2회차 · 영역: serve.py Handler 입력 검증

**고침 · HEAD 요청이 두 방어를 모두 우회 (보안)**
`do_GET` 에만 검사를 걸었는데 `SimpleHTTPRequestHandler` 가 `do_HEAD` 를 이미 갖고 있어서,
HEAD 로 전부 통과됐다. 실측:

    GET  /snapshots.db          404  차단
    HEAD /snapshots.db          200  ← Content-Length 286720 노출
    HEAD Host:evil /index.html  200  ← DNS 리바인딩 방어 우회

본문은 안 나가지만 파일 존재·크기가 새고, 로컬 전용 관문이 무력화된다.
→ `do_HEAD` 를 같은 검사(Host + SERVED 화이트리스트)를 거치도록 재정의.
재검증: GET/HEAD 4개 경로 × 정상/공격자 Host 전부 동일하게 동작.

**검증 추가** — `Handler` 에 정의된 모든 `do_*` 메서드 소스에 `local_only` 가 들어있는지
`inspect` 로 확인. 앞으로 메서드를 추가하면서 검사를 빠뜨리면 self-test 가 잡는다.

**문제 없던 것** `/api/trade` 파라미터 경계를 훑었으나 결함 없음.
url 없음/빈값/8KB/개행 삽입/스킴 없는 `//host/...` 전부 호스트 화이트리스트가 네트워크 이전에 차단.
경로 탈출 `/../serve.py` 404. `limit` 은 음수→1, 거대값→100, 문자→기본값으로 정상 클램프.

---

## 2026-08-22 08:0x — 1회차 · 영역: 옵션 key 정규화 / PRIORITY_STATS

**고침 · 우선 옵션의 key 가 실제 옵션 key 와 달라 페이지 중복 제거가 전부 빗나감**
`serve.py` 가 `key` 를 `label.format("#")` 로 그냥 내보내서 `치명타 확률 +#%` 가 나오는데, 실제
옵션에서 뽑은 key 는 `치명타 확률 #%` 였다(카탈로그가 부호를 안 씀). 5종 전부 불일치.
결과: 페이지가 "측정"과 "표본"을 다른 옵션으로 보고 같은 옵션을 두 줄로 늘어놓았다.
→ `key` 를 `mod_key()` 에 통과시키도록 수정. 브라우저에서 확인: 조건 목록 21행 → 17행,
중복 4행(치명타 확률·치명타 피해 보너스·투사체 스킬 레벨·정확도의 표본 중복) 제거됨.

**고침 · 생명력 흡수 라벨이 거래소에 없는 문구**
`생명력 흡수 {}%` 로 적혀 있었으나 실제 옵션 문구는 `물리 공격 피해의 #%를 생명력으로 흡수`.
정규화를 거쳐도 매칭이 안 돼 이 조건만 수집 결과 0점이었다.
→ 라벨을 실제 문구로 교체(문턱값 6). 관측 n=116, 값 범위 5~9.88 로 매물은 충분하다.

**검증 추가** — PRIORITY_STATS 의 모든 라벨이 이미 정규형인지, 부호가 남지 않는지 self-test 에서 확인.

**남긴 가설(네트워크 필요, 이번 회차 범위 밖)**
`정확도 +100` 이 매물 1개뿐인 건 문턱값 문제가 아니라 stat id 가 전역 정확도(`explicit.stat_803737631`)
라서일 가능성이 크다. 관측값은 10~483(n=121)로 100 이상이 흔하다. 카탈로그에 `정확도 #(특정)`
(`explicit.stat_691932474`)이 따로 있고 "(특정)"이 로컬 변형으로 보인다 — 치명타에서 전역/로컬을
혼동해 0건이 나왔던 것과 같은 유형. 다음 수집 때 검색으로 확인할 것.

**참고** `민첩 #(측정)` 이 아직 목록에 있는 건 코드 결함이 아니라, 그 수집이 제외 규칙보다 먼저
시작됐기 때문이다. 다음 수집에서 사라진다.
